# member_util.py

This module provides utilities for generating member reports from team member data.

## Functions

### get_member_report(members) -> List[MemberDTO]
Serializes a list of members and returns a list of DTOs for reporting purposes.
- **Connections:** Uses `MemberSerializer` and `MemberDTO`.

## Notes
- Used to generate structured reports of team members for scenario analysis.
