# util.py

This module provides a simple utility function for probability calculations.

## Functions

### [[probability]](p: float) -> int
Returns `True` with probability `p` and `False` with probability `1-p`.
- **Connections:** Uses Python's `random` module.

## Notes
- Used for random chance calculations in simulation logic.
